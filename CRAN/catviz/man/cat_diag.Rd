% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cat_diag.R
\name{cat_diag}
\alias{cat_diag}
\title{General diagnostic helper for CAT specifications}
\usage{
cat_diag(spec, outcome = NULL, method = c("event", "drddd", "csdid"), ...)
}
\arguments{
\item{spec}{A \code{cat_spec} object}

\item{outcome}{Outcome variable name (required for \code{"drddd"} and \code{"csdid"})}

\item{method}{Diagnostic type: \code{"event"}, \code{"drddd"}, or \code{"csdid"}}

\item{...}{Additional arguments passed to the specific diagnostic function
(e.g., \code{pre_window})}
}
\value{
A list with diagnostic results (always includes \code{data}; includes
\code{plot} for \code{"drddd"} and \code{"csdid"})
}
\description{
Automatically selects an appropriate diagnostic based on the method argument.
Dispatches to:
\itemize{
\item "event" -> \code{\link[=cat_event_table]{cat_event_table()}} (event-time counts; no outcome needed)
\item "drddd" -> \code{\link[=cat_pt_drddd]{cat_pt_drddd()}} (subgroup pretrend plot)
\item "csdid" -> \code{\link[=cat_pt_csdid]{cat_pt_csdid()}} (parallel-gaps plot)
}
}
\examples{
df <- data.frame(id=rep(1:10,each=6), year=rep(2015:2020,10),
                 g=c(rep(2018,30),rep(Inf,30)), outcome=rnorm(60))
spec <- cat_spec(df, id="id", time="year", g="g")
result <- cat_diag(spec, method = "event")
}
