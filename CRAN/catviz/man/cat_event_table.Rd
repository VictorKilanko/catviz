% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cat_diag.R
\name{cat_event_table}
\alias{cat_event_table}
\title{Count treated and control observations by event time}
\usage{
cat_event_table(spec, event_window = -10:10)
}
\arguments{
\item{spec}{A \code{cat_spec} object (from \code{\link[=cat_spec]{cat_spec()}})}

\item{event_window}{Integer vector of event times to include (default \code{-10:10})}
}
\value{
A tibble with columns \code{e}, \code{n_treated}, \code{n_control}
}
\description{
Summarizes the number of treated and control observations by event time
\code{e = t - g}. Works directly with any \code{cat_spec} object; does not require
any additional labeling.
}
\examples{
df <- data.frame(
  id   = rep(1:10, each = 6),
  year = rep(2015:2020, 10),
  g    = c(rep(2018, 30), rep(Inf, 30))
)
spec <- cat_spec(df, id = "id", time = "year", g = "g")
cat_event_table(spec, event_window = -3:2)
}
