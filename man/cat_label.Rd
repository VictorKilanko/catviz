% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cat_label.R
\name{cat_label}
\alias{cat_label}
\title{Label CAT nodes with cohort letters and canonical g labels}
\usage{
cat_label(spec)
}
\arguments{
\item{spec}{A \code{cat_spec} object (from \code{\link[=cat_spec]{cat_spec()}})}
}
\value{
The same \code{cat_spec} object with three new columns added to \code{spec$data}
}
\description{
Adds three columns to \code{spec$data}:
\itemize{
\item \code{.cohort_letter}: uppercase letter assigned chronologically to each finite g cohort
\item \code{.g_label}: canonical label (\code{"g = 2015"} or \code{"g = ∞"})
\item \code{.g_pretty}: combined label (\code{"g = 2015 (A)"})
}
}
\details{
Never-treated units (\code{g = Inf} or \code{g \%in\% never_treated_values}) are
labeled with the last letter in the sequence.
}
\examples{
# After building a spec, label it:
# spec <- cat_spec(mydata, id="id", time="year", g="g")
# spec <- cat_label(spec)
}
