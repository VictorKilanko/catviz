% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cat_plot_tree.R
\name{cat_plot_tree}
\alias{cat_plot_tree}
\title{Plot a Causal Assignment Tree (unified interface)}
\usage{
cat_plot_tree(spec, counts = TRUE, grayscale = FALSE, save_plot = NULL, ...)
}
\arguments{
\item{spec}{A \code{cat_spec} object (from \code{\link[=cat_spec]{cat_spec()}}).}

\item{counts}{Logical; include sample-size counts in node labels (default \code{TRUE}).}

\item{grayscale}{Logical; use a grayscale color palette suitable for
black-and-white publications (default \code{FALSE}). When \code{TRUE}, treated
nodes are shown in dark gray and control nodes in light gray.}

\item{save_plot}{Optional file path to save the plot (e.g. \code{"tree.png"}).
Passed to \code{ggplot2::ggsave()}.}

\item{...}{Additional arguments passed to the underlying plot function.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{cat_plot_tree()} is the recommended high-level function for visualizing
any CAT design. It inspects the \code{cat_spec} object and automatically
dispatches to the correct underlying plot function:
}
\details{
\tabular{lll}{
   Design \tab Condition \tab Underlying function \cr
   \strong{DDD / DR-DDD} \tab \code{subgroup} column provided, multiple \code{g} values \tab \code{cat_plot_ddd()} \cr
   \strong{CSDiD} \tab No subgroup, multiple \code{g} values \tab \code{cat_plot_csdid()} \cr
   \strong{2×2 DiD} \tab No subgroup, exactly one finite \code{g} value \tab \code{cat_plot_did()} \cr
}
}
\examples{
# --- CSDID example ---
df <- data.frame(
  id   = rep(1:6, each = 4),
  year = rep(2017:2020, 6),
  g    = c(rep(2018, 8), rep(2019, 8), rep(Inf, 8))
)
spec <- cat_spec(df, id = "id", time = "year", g = "g")
cat_plot_tree(spec)

# --- DDD example ---
df$p <- rep(c(0L, 1L), 12)
spec_ddd <- cat_spec(df, id = "id", time = "year", g = "g", subgroup = "p")
cat_plot_tree(spec_ddd)

# --- Grayscale (for B&W publications) ---
cat_plot_tree(spec, grayscale = TRUE)
}
