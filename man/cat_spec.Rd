% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cat_spec.R
\name{cat_spec}
\alias{cat_spec}
\title{Build a Causal Assignment Tree specification}
\usage{
cat_spec(
  data,
  id,
  time,
  g,
  subgroup = NULL,
  group_id = NULL,
  never_treated_values = c(0, Inf)
)
}
\arguments{
\item{data}{A data frame (panel structure: one row per unit × time period).}

\item{id}{Name of the \strong{unit} identifier column (e.g. \code{"hospital_id"}).}

\item{time}{Name of the \strong{time} column (e.g. \code{"year"} or \code{"date"}).}

\item{g}{Name of the \strong{first treatment period} column. Units that never
receive treatment should have \code{g = Inf} (or another value listed in
\code{never_treated_values}).}

\item{subgroup}{\emph{(optional)} Name of a binary subgroup column (0/1), used
for \strong{DR-DDD} designs. Omit or set \code{NULL} for pure \strong{CSDID}.}

\item{group_id}{\emph{(optional)} Name of a higher-level grouping column (e.g.
\code{"state"}) when treatment is assigned at a level above the unit.}

\item{never_treated_values}{Numeric vector of \code{g} values that indicate
never-treated status. Default: \code{c(0, Inf)}.}
}
\value{
A \code{cat_spec} object: a list with elements
\itemize{
\item \verb{$data}  — the augmented data frame
\item \verb{$meta}  — a list of variable names and settings
}
}
\description{
\code{cat_spec()} is the entry point for \code{catviz}. It attaches internal
standardised columns (\code{.id}, \code{.time}, \code{.g}, \code{.subgroup}, \code{.NT}, \code{.NYT},
\code{.node}) to your panel data and records the variable mapping so that all
downstream functions know where to look.
}
\examples{
# Minimal CSDID example
df <- data.frame(
  id   = rep(1:4, each = 3),
  year = rep(2018:2020, 4),
  g    = c(rep(2019, 6), rep(Inf, 6))
)
spec <- cat_spec(df, id = "id", time = "year", g = "g")

# DR-DDD example with subgroup
df$p <- rep(c(0, 1), 6)
spec_ddd <- cat_spec(df, id = "id", time = "year", g = "g", subgroup = "p")
}
